extends ../layout


block content
    each libro in libros
        h1
            a(href="/libros/d/" + libro.lib_id ) #{libro.titulo}
        small= 'Autor: ' + libro.autor + 	" \u2022 " + libro.genero
        p=  'Descripcion: ' + libro.descripcion